# Copy to `.env` and point at your Strapi instance.

NODE_ENV=development
HOST=localhost
SERVER_URL=http://localhost:1337

# Optional: used only for server-side AI features, never exposed to the browser
GEMINI_API_KEY=
